- name: Consumed Energy
  unique_id: consumed_energy
  device_class: energy
  state_class: total_increasing
  unit_of_measurement: kWh
  state: >
    {{ states('sensor.breaker_energy') | float | round(3) }}
  availability: >
    {{ has_value('sensor.breaker_energy') }}
